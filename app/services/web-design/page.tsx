import type { Metadata } from "next";import {ServiceDetail} from "@/components/service-detail";import {pageMetadata} from "@/lib/metadata";
export const metadata:Metadata=pageMetadata({title:"Web Design & Development in Bruges, Belgium",description:"Distinctive responsive and accessible web design and development from Bruges.",path:"/services/web-design"});
export default function Page(){return <ServiceDetail eyebrow="Web design & development / Bruges" title="Websites built" accent="around the work." intro="Structure, interface and code are developed together so the site fits the content, the brand and the devices people actually use." heading="Responsive, accessible and ready to ship." paragraphs={["DeeQ builds company sites, artist platforms, portfolios, booking flows and custom digital experiences.","Responsive behaviour, accessibility, performance, search foundations and handoff are part of the build, not a final checklist."]} markers={["Direction","Interface","Development","QA"]}/>}
